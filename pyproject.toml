[project]
name = "weighted-safety-refusal"
version = "0.1.0"
description = "Weighted Safety Refusal evaluation suite for Inspect AI"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "inspect_ai>=0.3.0",
]
